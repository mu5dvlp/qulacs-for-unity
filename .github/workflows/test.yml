name: Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read
  checks: write
  pull-requests: write

jobs:
  test:
    name: EditMode Tests (Unity ${{ matrix.unity-version }})
    runs-on: windows-2022

    strategy:
      fail-fast: false
      matrix:
        unity-version:
          - 2020.3.48f1
          - 2021.3.39f1
          - 2022.3.70f1
          - 6000.0.47f1

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Unity ライセンスは GitHub Secrets に登録が必要:
      #   UNITY_LICENSE  : Unity Personal/Pro ライセンスの XML 文字列
      #   UNITY_EMAIL    : Unity アカウントのメールアドレス
      #   UNITY_PASSWORD : Unity アカウントのパスワード
      - name: Activate Unity and run tests
        uses: game-ci/unity-test-runner@v4
        env:
          UNITY_LICENSE:  ${{ secrets.UNITY_LICENSE }}
          UNITY_EMAIL:    ${{ secrets.UNITY_EMAIL }}
          UNITY_PASSWORD: ${{ secrets.UNITY_PASSWORD }}
        with:
          unityVersion:   ${{ matrix.unity-version }}
          testMode:       editmode
          artifactsPath:  test-results
          githubToken:    ${{ secrets.GITHUB_TOKEN }}

      - name: Upload test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: test-results-${{ matrix.unity-version }}
          path: test-results
